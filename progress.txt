Task: Implement Pricing Logic
Status: Completed
Current Action: Implemented dynamic pricing logic for late checkout extension requests based on hour duration.
Next Action: Implement Payment Integration (Stripe).
Notes:
- Implemented `InvalidRequestedTimeError` in `src/late_checkout/core/services.py` and caught it as HTTP 400 Bad Request in `src/late_checkout/api/routers/extension_requests.py`.
- Used offset-aware datetime comparisons (converting SQLite naive datetimes to timezone-aware where necessary).
- Calculated pricing logic based on hours difference, rounding up partial hours using `math.ceil()`, and applying $20/hour rate.
- Added tests for successful pricing calculation and validation failures (past date, before original checkout) in `tests/test_api_extension_requests.py`.
- All Validation Gates passed successfully (`pytest`, `mypy`, `flake8`, `radon`, `pydeps`).
