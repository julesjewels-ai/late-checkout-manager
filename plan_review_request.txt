Hi, I have reviewed the tasks for the Late Checkout project and have identified the next task.

1. Implement Extension Request API:
The task requires implementing an Extension Request API.
This requires us to add an endpoint to create and view Extension Requests.
For Clean Architecture and SOLID principles, I will separate the concerns by doing the following:
- Create `ExtensionRequestCreate` and `ExtensionRequestResponse` Pydantic models (DTOs) in `src/late_checkout/domain/schemas.py`.
- Create a service layer for handling extension requests in `src/late_checkout/core/services.py` (e.g. `create_extension_request`, `get_extension_request`).
- Create an API router in `src/late_checkout/api/extension_requests.py` for `/extensions` with `POST` and `GET` methods.
- Include the API router in `src/late_checkout/main.py`.

SCP Risk Analysis:
- Input Validation (Score 4): We need to validate requested_time is in the future.
- SQL Injection (Score 4): Using SQLAlchemy ORM mitigates this.
- Authorization (Score 3): Out of scope for this specific task (marked as a separate future phase), but we will assume an endpoint without auth for now.

Are there any other specific security controls or validations you want me to add? I will set this plan in my tools.
